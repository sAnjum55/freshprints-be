export interface ApparelManipulationParams{
    id: string
    size: string
    quantity: string
    price: string
}

export interface OrderItem {
    id: string;
    size: string;
    quantity: number;
  }
  