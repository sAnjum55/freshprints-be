export interface ApparelManipulationParams{
    id: string
    size: string
    quantity: number
    price: number
}

export interface OrderItem {
    id: string;
    size: string;
    quantity: number;
  }
  